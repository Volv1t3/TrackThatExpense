<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TrackThatExpense" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
<!--        Esta linea nos permite manejar el estilo de la navegacion, interesantemente, si la eliminamos regresamos a un estilo
prestablecido por material UI, feo.-->
        <item name="navigationViewStyle">@style/CustomNavigationView</item>
    </style>
    <style name="Theme.TrackThatExpense.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.TrackThatExpense.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="Theme.TrackThatExpense.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>


    <style name="NavigationDrawerText" parent="TextAppearance.MaterialComponents.Body1">
        <item name="android:textSize">12sp</item>
        <item name="android:fontFamily">@font/inter_light</item>
        <item name="android:textColor">@color/textColor</item>
    </style>
    <style name="transparentAppButton">
        <item name="android:background">@android:color/transparent</item>
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="rippleColor">@android:color/darker_gray</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:padding">0dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:insetLeft">0dp</item>
        <item name="android:insetRight">0dp</item>
        <item name="cornerRadius">0dp</item>
        <item name="strokeWidth">0dp</item>
        <item name="elevation">0dp</item>
        <item name="strokeColor">@color/white</item>
    </style>

    <style name="displayPaneForContent">
        <item name="android:background">@color/highlightOne</item>
        <item name="android:padding">5dp</item>
    </style>
</resources>